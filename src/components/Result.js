import React from "react"

/**
 * This component will be where you will put the 
 * code to show the table for youtube videos in a given channel
 * and the associated information (as per the ASSIGNMENT.MD) file
 */
const Result = (props) => {
    //put any states that you need to keep track of here

    //put any helper functions you will use here


    //put code to render the table in this return
    return (
        <div align="center-left">
            <p>Display result table here!</p>
        </div>
    )
}

export default Result;